# CONTEXT — 领域词汇表

本文件**只是词汇表**：不含实现细节、不是规格、不是草稿本。
实现规格在 `docs/文档.md`；当前决策在 `.scratch/game-creation-v1/map.md`。

术语若与 `docs/文档.md` 有出入，以本文件为准（本文件记录的是本项目**实际采用**的含义）。

> ⚠️ **2026-09-24 重画**：终点从「一条命令跑通全链路 + 自我修复」改成
> 「**两个可独立交付的产物**（游戏资源包 / 可运行 demo）」。词汇表随之重画：
> 与闭环（评分、门禁、修复、运行时观察）相关的一整簇术语**已判出局**，
> 集中收在文末的「已出局」一节，不再代表本项目采用的含义。
> **Asset 的定义被推翻**（见下），这是本次重画改动最大的一条。

---

## 产物与生命周期

**Creation Project（创建项目）**
顶层容器：一个标题、一段需求文本、零到多张风格参考图。
⚠️ 其下属的 Run / Attempt / Iteration 三层结构属于旧的闭环模型，**已出局**；
新的产物粒度（一次资源包生成算不算一个「Run」）**待重定**，归票 24 / 28。

**Artifact（产物）**
文件系统上的实际内容。**绝对禁止覆盖式生成**：`cow/v1`、`cow/v2` 并存，
旧版本永久保留。这条性质在重画后**仍然成立**，只是对象从「run 内的中间产物」
变成了「**交付出去的资源包**」。

⚠️ **2026-09-24（票 24）粒度落定**：版本目录是 **per-pack**（`<packId>/v<N>/`），
**不是** per-asset。`docs/文档.md` 第 21 节的 `cow/v1, cow/v2` 是旧粒度的写法。
理由：会坏掉、会被回滚、会被复现的是**一整个交付物**，不是包里某个资源。

**ArtifactRef（产物引用）**
指向一个大型产物的**轻量引用**（id / type / path / version / checksum / createdAt）。
⚠️ **2026-09-24（票 24）已被取代**：资源包里**不用** ArtifactRef。
包内每个文件只需要 `{path, bytes, checksum}` —— `version` 与 `createdAt` 是**包级**的，
挂在每个文件上是过度建模。ArtifactRef 若将来还活着，是给包**之间**的引用用的。
校验和算法定为 **`sha256:<hex>`，逐文件**，覆盖包内全部文件（`manifest.json` 自身除外）。

---

## 编译产物（Contracts / ABI）

各阶段之间**只通过 Contract 通信**，任何模块不得依赖另一个模块的内部实现。

**StyleSpec（风格规格）**
从参考图提取出的**视觉语法**，不是参考图的描述，更不是参考图的复制。
回答「这个世界长什么样、按什么规则长」：色板、形状语言、材质、光照、镜头、构图。
关键性质：它必须能约束**参考图里没出现过的新事物**（参考图里没有牛，
但生成出来的牛必须属于同一个视觉世界）。这条性质是产物 A 验收第 2 条的判据来源。
**它是资源包的一部分**（票 24 定形状）。

**GameSpec（游戏规格）**
从需求文本推导出的**游戏设计**：核心循环、世界、玩家、机制、实体、场景、
UI、规则、胜负条件、需求清单。

**Requirement（需求）**
用户明确要求的一条能力，带优先级和验收标准。
⚠️ 重画后它不再驱动「门禁」（那是出局的）、而是驱动**游戏规格与资源清单**。

**AssetSpec（资源规格）**
一个待生成资源的**需求描述**：种类、外观、状态/动画、尺寸、依赖。
AssetSpec 是「需要什么」，Asset 是「实际做出的东西」。

**Asset Recipe（资源清单）**
一次资源生成要造哪些资源的完整清单。**它是文件**，先落盘再生产 ——
因此「人给的清单」与「从需求推导的清单」在下游完全同构（票 28 定形状）。
⚠️ **2026-09-24（票 24）**：本词一度与 AssetManifest 共用「资源清单」这个中文名，
已分开 —— **Recipe = 资源清单（要什么），Manifest = 资源包自描述（做出了什么）**。

**AssetPackManifest（资源包自描述）**
一个**资源包**的唯一入口：包里有哪些资源、各自什么种类、有哪些帧与动画、
文件在哪、checksum 多少、用的是哪份 StyleSpec、哪些是降级来的。
**产物 B 与 agent 都只读它**（票 24 定 schema，`format: "assetpack/v1"`）。
⚠️ **与 `docs/文档.md` 第 20 节的 `AssetManifest` 不是同一个东西** ——
那个是**输入侧**的资产集合 + 旧闭环的过程统计（`generated`/`missing`/`broken`/`unused`），
归 Recipe；R2 之后那三个计数器没有消费者了。
manifest **不重复图集里已有的内容**（帧的几何在交付态图集里）、
**不重复可派生的内容**（没有 `states[]`，「有哪些动画」= `animations[].name`）。

---

**Asset（资源）** ⚠️ 本项目特化含义 —— **2026-09-24 定义已推翻重写**

一个可直接被游戏引擎使用的游戏资源，是产物 A 的交付内容。它同时有**两种表达**：

- **创作态（Authoring Form）**：本项目生成与校验时使用的形式。
  drawlist JSON，或（对需要手绘质感的资源）原生位图源文件。
  **可 diff、可静态校验、可精确重放**。
- **交付态（Delivery Form）**：离开本项目时的形式。
  **位图 PNG + 标准图集/动画元数据** —— 任何引擎都能直接吃，
  不需要本项目的 runtime。

⚠️ **旧的表述「Asset 就是一份 DrawList，不是位图」已作废**。
作废原因：它把「本项目怎么表示」和「外部怎么使用」混成了一个概念，
导致「资源」无法作为独立产物交付。现在两者是同一条管线的两端，不是二选一的路线。

**「风格一致性可证明」这条性质仍然成立** —— 只是它现在成立于**创作态**
（色板引用使「颜色 ∈ 色板」构造上恒真），再由创作态**决定性地**传导到交付态。

⚠️ **2026-09-24 精确化**（[票 36](.scratch/game-creation-v1/issues/36-opacity-and-palette-invariant.md)）：
带 `opacity` 的 op 会做 alpha 混合、产出色板外颜色（实测 `hazard` 上有两个）。
裁决是**保留 `opacity`、把不变量说准**，现在是三段：

1. **来源层面（构造恒真）**：所有颜色**都必须是 `palette:N` 引用**，硬编码 hex 无法通过 schema。
2. **函数层面（构造恒真）**：渲染出的每个不透明像素颜色都是**色板颜色的确定函数**；
   `opacity` 只是这个函数里的一个标量参数，不引入任何色板之外的**来源**。
3. 失效的只是这个更强的说法：「渲染后的像素集合 ⊆ 色板」—— 它从来不是设计目标，
   只是第 2 条在「没有 opacity」时的偶然推论。

因此「换色板 → 创作态文本一字不变」与「换色板 → 交付态整体换色」**两条都还成立**。
推论：对 drawlist 资源，**[[Palette Binding]] 是解析期静态可判的**（看有没有 `opacity`），不需要渲染。

**Asset Kind（资源种类）**
资源的类别，四类一等公民：**sprite / animation / background / ui**。
它们不是同一种东西的不同尺寸 —— 背景是场景尺度、可能分层视差；
UI 是屏幕空间、可能九宫格可拉伸；animation 有帧与时间。
**map / 关卡不在此列**：它是「引用其他资源的布局」，归 Game Config。
各类的具体规格归票 27。

**DrawList（绘制指令表）** —— 现在是**创作态**的一种，不再是 Asset 的定义
一份 JSON，含 `format` / `id` / `state` / `viewBox` / `expectedSize` / `ops`。
`ops` 是一串**纯数值图元**（`rect` / `circle` / `ellipse` / `poly` / `line` / `curve`）。
关键性质：**不含任何不透明字符串** —— 连曲线都是数值控制点列，不是 SVG 的 `d`。
因此它的包围盒、用色、尺寸都能在**不渲染**的前提下算出来。
代价是包围盒为**凸包上界**（保守高估，只会误报过大、不会漏报过大）。

**Palette Reference（色板引用）**
DrawList 里表达颜色的唯一合法形式：`palette:N`，指向 `StyleSpec.palette` 的一项。
**硬编码色值不是「不推荐」，而是根本无法通过 schema** ——
「颜色 ∈ 色板」因此是一个**构造上恒真**的命题，而非运行时检查项。
推论：换掉整个 StyleSpec 色板，Asset 的**创作态**文本一字不变
（交付态会变，因为 PNG 像素确实变了 —— 这正是创作态/交付态分层的意义）。

**State（资源状态）** —— ⚠️ **2026-09-24 降格，不再是独立维度**（[票 26](.scratch/game-creation-v1/issues/26-animation-representation.md)）
一个「状态」就是**一个单帧动画**。番茄的 growing / ripe / harvested 是三个各含一帧的动画；
player 是 idle / walk / jump 三个动画。
理由：两者在数据上**没有可区分的差别**（一个命名外观 vs 一个可播放的序列），
而拆成两套概念要在 manifest、引用语法、装配代码里各走一遍。
**推论**：drawlist 文件里**没有 `state` 字段**，只有 `frame`（这一帧的名字）；
manifest 的引用语法从 `{asset, state?, anim?}` 收成 **`{asset, anim?}`**。
⚠️ 收掉它的直接原因：`state` 这个词在**真实产物里已经被用在两件事上** ——
style-transfer 的 `player.idle` 是静态外观，animated-player 的 `walk1` 是帧名，
两者却写进同一个字段。

**Palette Binding（色板绑定）**
一个资源**交付态像素颜色**与 `StyleSpec.palette` 的关系，三值且**每个资源必填**：
`exact`（每一帧的每个非透明像素颜色 ∈ 色板，**光栅化后扫像素实测**，不是承诺）、
`quantized`（交付前被最近邻量化到色板，原始色留在 `authoring/`）、
`unbound`（不量化，**必须在降级记录里留一条**，否则 schema 拒绝）。
存在的理由：**人工导入的位图颜色不受色板约束** ——
「颜色 ∈ 色板」对它们不成立，包契约必须把这件事显式化，而不是假装它不存在。

**Provenance（来源标注）**
一个资源/一个包**是谁造的**，必填且由 schema 强制自洽：
资源级 `origin` ∈ `generated`（本管线从规格产出）/ `imported`（人工导入通道）/ `fixture`（仓库内置降级件）；
包级 `mode` ∈ `generated` / `fixture` / `mixed`，**由 `origin` 唯一派生** ——
所以「一个 fixture 包谎报自己是 generated」是**解析不通过**，不是一条能被忽略的约定。
**一个不知道自己是 fixture 的包是虚假的包。**

**Frame（帧）**
一个 Asset 的一格画面。创作态是一条 drawlist，交付态是图集里的一帧。
帧的**名字**是它在 Asset 内的标识（`player.walk1`），也是产物 B 引用它的名字。
⚠️ **帧自己不带锚点、不带时长** —— 锚点见 [[Anchor]]，时长属于 [[Animation]] 的播放参数。

**Animation（动画）**
**一个 Asset 内部唯一的分组概念**：一组**有序**的 [[Frame]] + 播放参数（fps / loop）。
至少一帧。它是一等资源种类（见 [[Asset Kind]]）。
播放参数**资源给默认，[[Game Config]] 可以逐引用覆盖** ——
这样同一个角色在两个游戏里能用不同速度，而资源单独拿出去也是自描述的。

**Anchor（锚点）**
一个 Asset 的**逐资源**归一化定位点，写进图集 JSON 的**每一帧**
（交付格式支持逐帧，我们只是不需要逐帧变）。
⚠️ **它必须被声明，不能从 ink 包围盒自动推** —— 实测证据：player 六帧里
jump 的包围盒底边比其余五帧高 2px（收腿腾空），自动推会把它钉回地面、**腾空动作消失**，
而其余五帧看起来完全正常。这类错误不会自己暴露。

---

**Game Config（游戏配置）**
AI 产出的**纯数据**，描述实体清单、场景布局、交互规则、胜负条件。
它是 AI 与手写游戏代码之间**唯一的语义接口** —— 重画后这条性质更强了：
AI **连纯函数都不写**（只产数据），所以凡是 runtime 需要知道的东西，
没有第二个地方可以承载。

⚠️ **map / 关卡属于 Game Config，不是一种 Asset。**
理由：它是「引用其他资源的布局」，不是可绘制产物 ——
drawlist 里没有「实例化另一个资源」这种 op，所以场景组合无法用 drawlist 表达。

---

## 交付与调用

**Asset Pack（资源包）** —— ⚠️ **它就是「产物 A」**（地图 Destination 里的叫法）
产物 A 的交付物：一个自包含的目录，装着交付态位图、创作态源文件、
StyleSpec 与自描述。**它必须能被一个完全不了解本项目的引擎直接使用。**
⚠️ **2026-09-24（票 24）结构已定**：

```
<包根>/manifest.json          唯一入口
      delivery/               交付态：引擎直接吃，不需要本项目 runtime
        atlas.<kind>.json .png    按 kind 各一份 TexturePacker JSON Hash（票 25 的事实形状）
      authoring/              创作态：本项目读，可 diff、可静态校验、可重生成
        stylespec.json / drawlist/<frameName>.json / imported/<assetId>.<ext>
```

顶层按**表达层次**分（而不是按资源种类分）：两种消费者的边界就是这条分界线，
且两层可以各自缺失。交付态内部**按 kind 分图集文件**：四类不是同一种东西的不同尺寸，
最优打包参数不同（`ui` 要九宫格、`background` 是可平铺大图、`animation` 逐帧带锚点）。
包里**不含 game-config** —— 那是 Game Config 的产物、产物 B 的输入。
一个包 = 一份自洽的资源集合，**不绑定游戏**。

**Delivery Format（交付格式）**
交付态位图所配的图集 / 动画元数据格式。要求是**引擎中立**，
具体选哪个标准归票 25（事实）与票 24（决策）。

**Demo Site（可运行 demo / 站点）** —— ⚠️ **它就是「产物 B」**（地图 Destination 里的叫法）
一个**静态可托管、打开即玩**的小型横道游戏目录。由**手写固定、永不改变的 runtime 外壳**
加 AI **只产出的数据**装配而成 —— 一笔代码都不生成（R8）。
「打开即玩」的准确含义是**起一个静态 HTTP server 之后打开**，不是 `file://` 双击
（两个互相独立的原因，见[票 03](.scratch/game-creation-v1/issues/03-phaser-vite-playwright-chain.md)）。
它**不吃生图能力** —— 喂一个 [[Fixture Asset Pack]] 也能跑（R6）。
⚠️ 曾与仓库根那个 `demo/` 目录同名不同物（那是上一版闭环的 mock 骨架，已于 2026-09-24 拆解删除）。

**Fixture Asset Pack（夹具资源包）**
仓库内置的一份资源包，用于在**不碰生图能力**的前提下跑通产物 B。
存在的理由：产物 B 的验收是「喂 fixture 资源包也能跑」——
一个不知道自己是 fixture 的产物是虚假的产物，因此降级标注必须显式。

**Degradation（降级）**
生成侧的回退：生图 / 视觉模型 / 文本模型三条上游独立可达性，
任一条不可用时的回退规则。归票 14。
⚠️ 它**不再是「运行侧降级」** —— 没有 run 了。

---

## 已出局（本 effort 不采用，词汇保留供将来的闭环地图使用）

以下术语曾是本项目的核心词汇，2026-09-24 随终点重画整体出局。
**它们不代表本项目当前采用的含义**，列在此处只为在别处读到时不致误解。

- **Creation Run / Iteration / Attempt / Lifecycle State** —— 闭环模型的生命周期载体。
- **Runtime Observation / RuntimeEntity / Runtime Event Log / Runtime Black Box** —— 游戏运行时的语义快照与事件流。
- **Runtime Bridge（运行时桥）** —— 游戏内部暴露语义状态的通道。
- **Semantic Test Action（语义测试动作）** —— 以游戏语义而非屏幕坐标表达的测试操作。
- **GameplayTestPlan / Critical Path** —— 玩法测试计划与关键路径。
- **Visual QA / Gameplay QA / Requirement QA / Runtime QA** —— 各维度质检。
- **Evidence（证据）** —— QA Issue 携带的可追溯凭据。
- **Creation Gate / Hard Gate / Quality Score / Blocker** —— 门禁与评分。
- **Repair / RepairAction / RepairProgress / Stalled / Best State / Budget** —— 修复循环。
- **Checkpoint** —— 阶段状态 dump（原只做写入，不做 Resume）。

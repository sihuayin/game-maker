# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo

# 产物 A 与产物 B —— 全部 build 出来的东西都在这，不入库（Q9 精神：
# 只有 inputs/ 与 fixtures/ 入库）。见票 29。
out/

# 本地凭据 —— **永不入库**（生图上游的 baseUrl 与 key，见 packages/assets/src/image-config.ts）
game-maker.local.json
*.local.json

# Logs
*.log

# macOS
.DS_Store
